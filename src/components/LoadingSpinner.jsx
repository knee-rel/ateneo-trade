import React from "react";
import { Spinner } from "react-spinner-animated";

const LoadingSpinner = () => {
  return <Spinner center={true} width={"150px"} height={"150px"} />;
};

export default LoadingSpinner;
